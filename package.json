{
  "name": "kanjoplayer-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w kanjo-player-demo",
    "build": "npm run build -w kanjo-player",
    "build:all": "npm run build -w kanjo-player && npm run build -w kanjo-player-demo",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}
